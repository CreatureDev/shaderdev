GUI
	check boxes
	file explorer
	value fields
	drop down menus (depth testing, alpha blending)
	

types of input values
	OpenGL state (depth test, alpha blend, draw mode)
	vertex attributes
	uniform values
		type + value
	buffers
		size + fill value or file
	textures (uniform)


types of ignored input
	varying attributes
		i.e. in values for fragment shader

special datas
	Vertex
		draw mode
	Compute
		frequency (every frame vs once)

	

