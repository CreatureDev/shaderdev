#ifndef __INPUTGUI_H__
#define __INPUTGUI_H__

/* Draw laws
 * Multi dimension field value
 * Toggle
 * Field value
 *	
 * File value
 *	valid extension
 * Dropdown
 *	list of valid states
 * Variable dimension field value
 *	list of valid states
 * Buffer value
 *	buffer size 
*/


struct 

/*
struct drawlaw
{
	char *name;
	void *dat;
	unsigned int ina;
	unsigned int inb;
	struct drawlaw *dla;
	struct drawlaw *dlb;
};


struct drawlawlst
{
	unsigned int id;
	unsigned int st;
	void (*enfunc)(void);
	void (*difunc)(void);
	struct drawlawlst *next;
	struct drawlaw *stst;
	struct drawlaw *crst;
};
*/

int initgui();

#endif

